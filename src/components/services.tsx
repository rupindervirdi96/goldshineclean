import React from "react";

interface ServicesProps {}

export const Services = ({}: ServicesProps) => {
  return (
    <div
      id="services"
      className="min-w-full h-[calc(100vh-68px)] flex items-center justify-center"
    ></div>
  );
};
